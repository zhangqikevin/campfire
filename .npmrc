# Pin to the public npm registry. Both the main SaaS app (src/) and the
# local-ui static export depend on @openuidev/* packages that live only on
# registry.npmjs.org — some Chinese mirrors don't sync them.
registry=https://registry.npmjs.org/
@openuidev:registry=https://registry.npmjs.org/
