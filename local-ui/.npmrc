# Same as plugin/.npmrc — pin to npmjs.org so the @openuidev/* renderer
# packages resolve even when the user's global registry points at a mirror
# that doesn't have them.
registry=https://registry.npmjs.org/
@openuidev:registry=https://registry.npmjs.org/
